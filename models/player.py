class Player:
    def __init__(self, id, player_id, team_name, player_name, rank, total_points):
        self.id = id
        self.player_id = player_id,
        self.team_name = team_name,
        self.player_name = player_name
        self.rank = rank,
        self.total_points = total_points
